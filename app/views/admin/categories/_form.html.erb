<%= form_for(admin_category , html: {class: 'form-horizontal'}) do |f| %>
  <% if admin_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_category.errors.count, "error") %> prohibited this admin_category from being saved:</h2>

      <ul>
      <% admin_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'control-label col-sm-2' %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: 'form-control' %>        
    </div>
  </div>

  <div class="form-group">
    <%= f.label :alias_name, class: 'control-label col-sm-2' %>
    <div class="col-sm-10">
      <%= f.text_field :alias_name, class: 'form-control' %>      
    </div>
  </div>
  <div class="form-group">
    <center>
      <%= f.submit "Tạo Danh Mục", class: 'btn btn-primary' %>      
    </center>
  </div>
<% end %>
